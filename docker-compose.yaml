version: '3.9'

services:
  web:
    build: .
    container_name: backup_restore
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app               
      - static_data:/app/static  
    env_file:
      - .env

volumes:
  static_data:
